---
trigger: always_on
---

Every task should be realized working in a new branch starting from a main one.
Always check if the local main is aligned with the origin one.
Each branch should have the task name.